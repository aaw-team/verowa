plugin.tx_verowa.view {
    // cat=plugin.tx_verowa/templates/b1; type=string; label=Fluid templates path
    templateRootPath =
    // cat=plugin.tx_verowa/templates/b1; type=string; label=Fluid partials path
    partialRootPath =
}
